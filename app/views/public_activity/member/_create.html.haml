%li.list-group-item
  %span.glyphicon.glyphicon-edit
  %small.text-muted= a.created_at.strftime('%H:%M:%S %-d %B %Y')
  %br/
  %strong= activity.owner ? activity.owner.email : 'Guest'
  - if a.trackable
    create the member #{link_to a.trackable.firstname, member_path(a.trackable)}.
  - else
    create the member that is currently deleted.

